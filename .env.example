# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=ella_rises

# Session Configuration (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=generate_a_random_secure_string_here

# Email Configuration (Amazon SES for AWS deployment)
# For local development, you can use Mailgun or any SMTP service
# For AWS Elastic Beanstalk, use Amazon SES (see AWS-DEPLOYMENT-GUIDE.md)
MAIL_HOST=email-smtp.us-east-1.amazonaws.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your_ses_smtp_username_here
MAIL_PASS=your_ses_smtp_password_here
MAIL_FROM="Ella Rises <noreply@ellarises.org>"
MAIL_TO=contact@ellarises.org

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit your actual .env file to git
# 2. Generate a strong SESSION_SECRET: openssl rand -base64 32
# 3. For AWS deployment, set these in EB Console > Configuration > Software
# 4. Change all passwords before deploying to production
