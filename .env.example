# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=ella_rises

# Session Configuration (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=generate_a_random_secure_string_here

# Mailgun SMTP Configuration (Contact Form)
# Sign up at https://www.mailgun.com/ and get your own credentials
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_SECURE=false
<<<<<<< HEAD
# Fill these with your own Mailgun SMTP credentials (sandbox or production).
# Sandbox domains can only send to authorized recipients unless the account is upgraded.
MAIL_USER=postmaster@your-mailgun-domain
MAIL_PASS=your_mailgun_smtp_password
MAIL_FROM="Ella Rises <postmaster@your-mailgun-domain>"
MAIL_TO=recipient@example.com
=======
MAIL_USER=postmaster@YOUR-SANDBOX-DOMAIN.mailgun.org
MAIL_PASS=your_smtp_password_here
MAIL_FROM="Your App Name <postmaster@YOUR-DOMAIN.mailgun.org>"
MAIL_TO=recipient@example.com

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit your actual .env file to git
# 2. Generate a strong SESSION_SECRET: openssl rand -base64 32
# 3. Use your own Mailgun account credentials
# 4. Change all passwords before deploying to production
>>>>>>> 6d01d43f906c0fa11cdd3e43af0cc99c8c7ddf8a
